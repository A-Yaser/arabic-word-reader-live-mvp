import {defineConfig} from 'vite';import react from '@vitejs/plugin-react';import devCerts from 'office-addin-dev-certs';const https=await devCerts.getHttpsServerOptions();export default defineConfig({plugins:[react()],server:{host:'localhost',port:3000,strictPort:true,https,proxy:{'/api':{target:'http://127.0.0.1:8000',changeOrigin:true,rewrite:p=>p.replace(/^\/api/,'')}}},build:{outDir:'dist',emptyOutDir:true}});
